[pytest]
DJANGO_SETTINGS_MODULE = config.settings
python_files = test_*.py
testpaths = apps
# Show local variables on failure and stop after 5 failures - a full cascade of
# errors from one broken fixture is noise, not information.
addopts = --maxfail=5 --strict-markers
